<!-- New transactor path is used for a confirmation of transaction for buyer -->

<div class = "container">
  <div class="row">
    <!-- Page will render the current user's portfolio condition on the left -->
    <div class = "col-md-3">
      <%= render 'shared/portfolioCondition' %>
    </div>
    <div class = "col-md-8">
      <h3 class = "text-center">Confirm The Following Transaction</h3>
      <%= render '/shared/shieldStock', :otm1 => Onthemarket.find(params[:onthemarket]) %>
      <br/>
      <%= render 'form' %>
    </div>
  </div>
</div>